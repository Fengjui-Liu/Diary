# 📱 日記本 Web 版部署指南

> 最簡單、最快速的部署方式 - 5分鐘內上線！

## 🎯 選擇部署方式

我推薦以下三種方式，從簡單到進階：

### ⭐ 方案一：Vercel（最推薦）
- ✅ 完全免費
- ✅ 自動 HTTPS
- ✅ 全球 CDN
- ✅ 一鍵部署
- ✅ 自動更新

### 🔥 方案二：Netlify
- ✅ 完全免費
- ✅ 易於使用
- ✅ 支持表單和函數

### ☁️ 方案三：Firebase Hosting
- ✅ Google 服務
- ✅ 與 Firebase 整合
- ⚠️ 需要配置 Firebase

---

## 🚀 方案一：使用 Vercel 部署（推薦）

### 準備工作

1. **前往 GitHub 把代碼推送到你的倉庫**（已完成 ✅）

2. **註冊 Vercel 帳號**
   - 前往 https://vercel.com
   - 使用 GitHub 帳號登入（最方便）

### 部署步驟

#### 方法 A：直接從 GitHub 導入（最簡單）

1. **登入 Vercel 後，點擊 "Add New Project"**

2. **導入你的 GitHub 倉庫**
   - 選擇 `Fengjui-Liu/Diary` 倉庫
   - 選擇 `claude/diary-app-development-jCnyR` 分支

3. **配置專案**
   ```
   Framework Preset: Other
   Root Directory: diary_app
   Build Command: flutter build web --release
   Output Directory: build/web
   ```

4. **添加環境變數**（暫時可以跳過，先部署再說）

5. **點擊 "Deploy"** - 完成！🎉

你的網站會在 `https://your-project.vercel.app` 上線

#### 方法 B：使用 Vercel CLI

```bash
# 1. 安裝 Vercel CLI
npm install -g vercel

# 2. 切換到專案目錄
cd /home/user/Diary/diary_app

# 3. 構建專案
flutter build web --release

# 4. 部署
cd build/web
vercel --prod
```

按照提示操作即可！

---

## 🔥 方案二：使用 Netlify 部署

### 步驟

1. **前往 Netlify**
   - 訪問 https://netlify.com
   - 使用 GitHub 登入

2. **導入專案**
   - 點擊 "Add new site" > "Import an existing project"
   - 選擇 GitHub 倉庫 `Fengjui-Liu/Diary`
   - 選擇分支 `claude/diary-app-development-jCnyR`

3. **配置構建設置**
   ```
   Base directory: diary_app
   Build command: flutter build web --release
   Publish directory: diary_app/build/web
   ```

4. **部署** - 點擊 "Deploy site"

完成！網站會在 `https://your-site.netlify.app` 上線

---

## ☁️ 方案三：使用 Firebase Hosting

### 步驟

```bash
# 1. 安裝 Firebase CLI
npm install -g firebase-tools

# 2. 登入 Firebase
firebase login

# 3. 切換到專案目錄
cd /home/user/Diary/diary_app

# 4. 初始化 Firebase
firebase init hosting

# 選擇：
# - 創建新專案或選擇現有專案
# - Public directory: build/web
# - Single-page app: Yes
# - 不要覆蓋 index.html

# 5. 構建專案
flutter build web --release

# 6. 部署
firebase deploy --only hosting
```

網站會在 `https://your-project.firebaseapp.com` 上線

---

## 📦 本地測試（部署前）

在部署前，先在本地測試 Web 版本：

```bash
# 方法 1: 使用 Flutter 內建服務器
cd /home/user/Diary/diary_app
flutter run -d chrome

# 方法 2: 構建並使用本地服務器
flutter build web --release
cd build/web
python3 -m http.server 8000

# 然後在瀏覽器打開 http://localhost:8000
```

---

## ⚙️ 重要配置

### 1. 修改 `web/index.html` 確保正確的基本路徑

你的項目中 `web/index.html` 應該已經有了，確認這行：

```html
<base href="/">
```

如果部署在子目錄，改為：
```html
<base href="/your-subdirectory/">
```

### 2. 關於 Firebase（可選）

**如果你暫時不想用 Firebase（雲端同步），可以：**

- ✅ 使用本地存儲（已實現的 SQLite + SharedPreferences）
- ✅ 只使用本地密碼認證
- ✅ 圖片存儲在瀏覽器本地

**這意味著：**
- 📱 數據只存在用戶的瀏覽器中
- 🔒 更注重隱私
- 💾 不需要配置雲端服務
- ⚠️ 清除瀏覽器數據會丟失日記（可以添加導出功能）

**如果想要雲端同步，才需要配置 Firebase：**

```bash
# 安裝 FlutterFire CLI
dart pub global activate flutterfire_cli

# 配置 Firebase
flutterfire configure
```

---

## 🎨 自定義域名（可選）

### Vercel
1. 進入專案設置
2. 選擇 "Domains"
3. 添加你的域名
4. 按照指示配置 DNS

### Netlify
1. 進入 Site settings > Domain management
2. 添加自定義域名
3. 配置 DNS

---

## 📊 部署後檢查清單

- [ ] 網站可以正常訪問
- [ ] 可以設置本地密碼
- [ ] 可以新增日記
- [ ] 可以選擇心情和天氣
- [ ] 可以查看日曆
- [ ] 圖片功能正常（如果使用本地存儲）
- [ ] 響應式設計在手機上正常
- [ ] 深色/淺色模式切換正常

---

## 🔧 常見問題

### Q: 部署後一片空白？
A: 檢查瀏覽器控制台（F12）的錯誤訊息，通常是路徑問題。

### Q: 圖片無法上傳？
A: 如果沒有配置 Firebase Storage，圖片會存在本地。確保開啟了瀏覽器的存儲權限。

### Q: 刷新頁面後數據丟失？
A: 這是正常的，因為使用本地存儲。如果需要雲端同步，需要配置 Firebase。

### Q: 手機上無法使用？
A: Flutter Web 完全支持移動瀏覽器。確保使用現代瀏覽器（Chrome/Safari）。

### Q: 可以安裝到桌面嗎？
A: 可以！這是一個 PWA（Progressive Web App），在支持的瀏覽器中會顯示"安裝"選項。

---

## 🎯 推薦流程（新手友好）

1. **先在本地測試**
   ```bash
   cd /home/user/Diary/diary_app
   flutter run -d chrome
   ```

2. **確認功能正常後，構建生產版本**
   ```bash
   flutter build web --release
   ```

3. **選擇 Vercel 部署**（最簡單）
   - 前往 https://vercel.com
   - 連接 GitHub
   - 導入倉庫
   - 點擊部署
   - 完成！✨

4. **分享給朋友測試**

5. **收集反饋後再決定是否需要：**
   - 添加 Firebase 雲端同步
   - 開發手機 APP
   - 添加更多功能

---

## 📈 下一步

部署成功後，你可以考慮：

1. **添加自定義域名**
2. **配置 Firebase 實現雲端同步**
3. **添加數據導出功能**（防止數據丟失）
4. **添加 Google Analytics**（了解使用情況）
5. **優化 SEO**
6. **添加 PWA 離線功能**

---

## 💡 小提示

- 🚀 **Vercel 會自動在每次 git push 後重新部署**
- 🔄 **每個 git 分支都會有獨立的預覽 URL**
- 📊 **Vercel/Netlify 都提供訪問統計**
- 🎨 **可以隨時更改配置和重新部署**

---

**準備好了嗎？開始部署你的日記本 Web 應用吧！** 🚀

有任何問題隨時問我！
